<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap
    PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN"
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="qrVerify">

    <typeAlias alias="qrVerifyBean" type="in.ac.dei.edrp.admissionsystem.Bean.qrVerifyBean"/>

    <!-- Fetch all programs -->
    <select id="getProgramList" resultClass="java.util.HashMap">
        SELECT program_id, program_name, program_description
        FROM program_master
        ORDER BY program_name
    </select>
   
    <!-- Insert QR scan log -->
    <insert id="insertQrVerify" parameterClass="qrVerifyBean">
        <selectKey resultClass="int" keyProperty="id">
            SELECT LAST_INSERT_ID()
        </selectKey>
        INSERT INTO qr_scan_log
        (application_number, program_id, verification_status, verified_by, verified_at)
        VALUES
        (#applicationNumber#, #programId#, #verificationStatus#, #verifiedBy#, NOW())
    </insert>

    <!-- Query student info by application number -->
    <select id="getStudentByApplicationNumber" parameterClass="map" resultClass="java.util.HashMap">
        SELECT stn.test_number AS rollNo,
               en.student_name AS name,
               stn.program_id AS programId,
               en.photo AS photoPath,
               en.signature AS signaturePath,
               stn.student_id AS studentId
        FROM student_test_number stn
        JOIN entity_student en ON stn.test_number = en.application_number
        WHERE stn.test_number = #applicationNumber#
          AND stn.program_id = #programId#
    </select>

    <!-- Update verification -->
    <update id="updateVerification" parameterClass="qrVerifyBean">
        UPDATE qr_scan_log
        SET verification_status = #verificationStatus#,
            verified_by = #verifiedBy#,
            verified_at = NOW()
        WHERE id = #id#
    </update>

</sqlMap>
